# Sanity Backend

Run the following command in this directory to initialize the Sanity Studio:

```bash
npm create sanity@latest -- --project 3x0p7z2a --dataset production --template clean
```

Follow the interactive prompts to log in and set up the project.
