---
layout: base.njk
title: "Soul States"
description: "An index of spiritual conditions."
---

<div class="container py-24">
  <div class="text-center mb-16">
    <h1 class="font-serif text-5xl mb-6">States of the Soul</h1>
    <p class="text-xl text-text-light font-serif italic max-w-2xl mx-auto">
      Navigate your internal condition with honesty.
    </p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
  {% for state in sanity.states %}
  <a href="/content/states/{{ state.slug.current }}/" class="group block h-full">
      <div class="bg-white border border-border-subtle rounded-lg p-8 flex justify-between items-start h-full transition-all duration-300 hover:shadow-lg hover:border-accent-blue/30 group-hover:transform group-hover:scale-101">
        <div class="pr-4">
          <h4 class="font-serif text-2xl mb-4 flex items-center gap-3 group-hover:text-accent-blue transition-colors">
            {% if state.data.icon == 'waves' %}≈{% endif %}
            {% if state.data.icon == 'cloud' %}☁{% endif %}
            {% if state.data.icon == 'battery' %}🔋{% endif %}
            {% if state.data.icon == 'u-turn' %}↺{% endif %}
            {{ state.data.title }}
          </h4>
          <p class="text-base text-text-light leading-relaxed mb-4">{{ state.data.definition }}</p>
          <span class="text-sm font-bold text-accent-blue group-hover:underline">Explore State →</span>
        </div>
      </div>
    </a>
    {% endfor %}
  </div>
</div>
